<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For Shree ✨ | 2026</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&family=Playfair+Display:ital,wght@0,700;1,700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-dark: #050505;
            --accent-primary: #ffb7c5; /* Rose Gold/Pink */
            --accent-secondary: #9d50bb; /* Soft Violet */
            --glass: rgba(255, 255, 255, 0.03);
            --glass-border: rgba(255, 255, 255, 0.1);
            --text-main: #ffffff;
            --text-muted: #a0a0a0;
            --font-sans: 'Inter', sans-serif;
            --font-serif: 'Playfair Display', serif;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            cursor: none; /* Custom cursor for 2026 feel */
        }

        body {
            background-color: var(--bg-dark);
            color: var(--text-main);
            font-family: var(--font-sans);
            overflow-x: hidden;
            line-height: 1.6;
        }

        /* --- Custom Cursor --- */
        #cursor {
            position: fixed;
            width: 20px;
            height: 20px;
            background: var(--accent-primary);
            border-radius: 50%;
            pointer-events: none;
            z-index: 9999;
            mix-blend-mode: difference;
            transition: transform 0.1s ease;
        }

        /* --- Background Canvas --- */
        #bg-canvas {
            position: fixed;
            top: 0;
            left: 0;
            z-index: -1;
            filter: blur(2px);
        }

        /* --- Layout Sections --- */
        section {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 2rem;
            position: relative;
        }

        /* --- Hero Section --- */
        .hero {
            text-align: center;
        }

        .hero h1 {
            font-family: var(--font-serif);
            font-size: clamp(3rem, 10vw, 8rem);
            line-height: 1.1;
            margin-bottom: 1rem;
            background: linear-gradient(to right, #fff, var(--accent-primary), #fff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 1.5s forwards ease-out;
        }

        .hero p {
            font-size: 1.2rem;
            color: var(--text-muted);
            letter-spacing: 4px;
            text-transform: uppercase;
            opacity: 0;
            animation: fadeInUp 1.5s 0.5s forwards ease-out;
        }

        /* --- Glass Cards --- */
        .card-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            max-width: 1200px;
            width: 100%;
        }

        .glass-card {
            background: var(--glass);
            backdrop-filter: blur(15px);
            border: 1px solid var(--glass-border);
            padding: 3rem;
            border-radius: 24px;
            transition: all 0.4s ease;
            opacity: 0;
            transform: translateY(50px);
        }

        .glass-card.visible {
            opacity: 1;
            transform: translateY(0);
        }

        .glass-card:hover {
            border-color: var(--accent-primary);
            transform: translateY(-10px) scale(1.02);
            box-shadow: 0 20px 40px rgba(0,0,0,0.4);
        }

        .glass-card h2 {
            font-family: var(--font-serif);
            margin-bottom: 1.5rem;
            color: var(--accent-primary);
        }

        /* --- Interactive Button --- */
        .btn-magical {
            background: transparent;
            border: 1px solid var(--accent-primary);
            color: #fff;
            padding: 1rem 2.5rem;
            font-size: 1rem;
            border-radius: 50px;
            margin-top: 2rem;
            cursor: pointer;
            position: relative;
            overflow: hidden;
            transition: 0.3s;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .btn-magical:hover {
            background: var(--accent-primary);
            color: var(--bg-dark);
            box-shadow: 0 0 30px var(--accent-primary);
        }

        /* --- Message Reveal Section --- */
        .reveal-box {
            text-align: center;
            max-width: 800px;
        }

        .reveal-box h2 {
            font-family: var(--font-serif);
            font-size: 3rem;
            margin-bottom: 2rem;
        }

        .shree-note {
            font-size: 1.5rem;
            font-style: italic;
            color: var(--text-muted);
            opacity: 0;
            transition: 2s ease;
        }

        .shree-note.show {
            opacity: 1;
            color: var(--text-main);
        }

        /* --- Floating Elements --- */
        .floating {
            animation: float 6s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* --- Preloader --- */
        #loader {
            position: fixed;
            inset: 0;
            background: var(--bg-dark);
            z-index: 10000;
            display: flex;
            justify-content: center;
            align-items: center;
            transition: opacity 1s ease;
        }

        .loader-content {
            text-align: center;
        }

        .heart-icon {
            font-size: 3rem;
            color: var(--accent-primary);
            animation: pulse 1.5s infinite;
        }

        @keyframes pulse {
            0% { transform: scale(1); opacity: 1; }
            50% { transform: scale(1.3); opacity: 0.7; }
            100% { transform: scale(1); opacity: 1; }
        }

        /* Mobile Adjustments */
        @media (max-width: 768px) {
            .hero h1 { font-size: 4rem; }
            .glass-card { padding: 1.5rem; }
            #cursor { display: none; }
        }
    </style>
</head>
<body>

    <div id="loader">
        <div class="loader-content">
            <div class="heart-icon">✨</div>
            <p style="margin-top: 1rem; letter-spacing: 2px;">PREPARING THE MAGIC...</p>
        </div>
    </div>

    <div id="cursor"></div>
    <canvas id="bg-canvas"></canvas>

    <main>
        <!-- Hero Section -->
        <section class="hero">
            <p class="floating">EST. 2026 EDITION</p>
            <h1>Happy Birthday, <br>Shree ✨</h1>
            <p>A journey through elegance and light.</p>
            <button class="btn-magical" onclick="scrollToContent()">Begin the Story</button>
        </section>

        <!-- The Journey -->
        <section id="journey">
            <div class="card-container">
                <div class="glass-card">
                    <h2>The Radiance</h2>
                    <p>In 2026, the world has many lights, but none shine as consistently and warmly as yours. Shree, your presence is a masterclass in grace.</p>
                </div>
                <div class="glass-card">
                    <h2>The Soul</h2>
                    <p>It's not just another year; it's another chapter of you being the most incredible person I know. Your kindness is your superpower.</p>
                </div>
                <div class="glass-card">
                    <h2>The Future</h2>
                    <p>May this year bring you the same level of joy you bring to everyone around you. The best is truly yet to come.</p>
                </div>
            </div>
        </section>

        <!-- Personal Message -->
        <section class="reveal-section">
            <div class="reveal-box">
                <h2>For Your Eyes Only</h2>
                <button class="btn-magical" id="revealBtn">Tap to Reveal My Heart</button>
                <div class="shree-note" id="secretNote">
                    <br>
                    "Shree, you are the stardust that makes the mundane look magical. 
                    Wishing you a year filled with laughter, deep peace, and endless love. 
                    Happy Birthday, special one. 💗"
                </div>
            </div>
        </section>

        <footer style="padding: 4rem; text-align: center; color: var(--text-muted); font-size: 0.8rem; letter-spacing: 2px;">
            DESIGNED SPECIALLY FOR SHREE • 2026
        </footer>
    </main>

    <script>
        // --- Preloader Logic ---
        window.addEventListener('load', () => {
            setTimeout(() => {
                const loader = document.getElementById('loader');
                loader.style.opacity = '0';
                setTimeout(() => loader.style.display = 'none', 1000);
            }, 2000);
        });

        // --- Custom Cursor Logic ---
        const cursor = document.getElementById('cursor');
        document.addEventListener('mousemove', (e) => {
            cursor.style.left = e.clientX + 'px';
            cursor.style.top = e.clientY + 'px';
        });

        // --- Canvas Background (Particle System) ---
        const canvas = document.getElementById('bg-canvas');
        const ctx = canvas.getContext('2d');
        let particles = [];

        function resize() {
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
        }

        window.addEventListener('resize', resize);
        resize();

        class Particle {
            constructor() {
                this.x = Math.random() * canvas.width;
                this.y = Math.random() * canvas.height;
                this.size = Math.random() * 2 + 0.5;
                this.speedX = Math.random() * 0.5 - 0.25;
                this.speedY = Math.random() * 0.5 - 0.25;
                this.opacity = Math.random();
            }

            update() {
                this.x += this.speedX;
                this.y += this.speedY;
                if (this.x > canvas.width) this.x = 0;
                if (this.x < 0) this.x = canvas.width;
                if (this.y > canvas.height) this.y = 0;
                if (this.y < 0) this.y = canvas.height;
            }

            draw() {
                ctx.fillStyle = `rgba(255, 183, 197, ${this.opacity})`;
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
                ctx.fill();
            }
        }

        function initParticles() {
            for (let i = 0; i < 100; i++) {
                particles.push(new Particle());
            }
        }

        function animateParticles() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            particles.forEach(p => {
                p.update();
                p.draw();
            });
            requestAnimationFrame(animateParticles);
        }

        initParticles();
        animateParticles();

        // --- Scroll Reveal ---
        const observerOptions = { threshold: 0.2 };
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.glass-card').forEach(card => observer.observe(card));

        // --- Interaction Logic ---
        function scrollToContent() {
            document.getElementById('journey').scrollIntoView({ behavior: 'smooth' });
        }

        const revealBtn = document.getElementById('revealBtn');
        const secretNote = document.getElementById('secretNote');

        revealBtn.addEventListener('click', () => {
            secretNote.classList.add('show');
            revealBtn.style.display = 'none';
            // Trigger Confetti-like effect
            createConfetti();
        });

        function createConfetti() {
            for(let i=0; i<50; i++) {
                setTimeout(() => {
                    const p = new Particle();
                    p.x = window.innerWidth / 2;
                    p.y = window.innerHeight / 2;
                    p.speedX = (Math.random() - 0.5) * 10;
                    p.speedY = (Math.random() - 0.5) * 10;
                    p.size = Math.random() * 5;
                    particles.push(p);
                }, i * 20);
            }
        }
    </script>
</body>
</html>